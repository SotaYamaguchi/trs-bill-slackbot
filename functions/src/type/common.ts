export type User = {
  real_name: string,
  profile: {
    image_192: string
  }
}

export type Work = {
  course: string,
  date: string
}

export type Course = {
  course: string,
  place: string,
  price: number
}